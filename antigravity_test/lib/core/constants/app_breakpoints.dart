/// Breakpoints responsivos centralizados para el proyecto.
class AppBreakpoints {
  AppBreakpoints._();

  /// Ancho máximo para considerar layout móvil.
  static const double mobile = 800;

  /// Ancho máximo para considerar layout tablet.
  static const double tablet = 1200;
}
